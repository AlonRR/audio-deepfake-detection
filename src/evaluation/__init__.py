"""Creation evaluation — the three metrics required by the brief.

    spectrogram_sim.py   Mel-cepstral distortion (MCD) + log-mel L2/SSIM.
    speaker_sim.py       ECAPA-TDNN speaker-embedding cosine similarity.
    mos.py               MOS listening-panel harness (+ optional auto-MOS proxy).
"""
